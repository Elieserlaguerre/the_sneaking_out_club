import ViewGroupLayout from "@/app/components/global-components/ViewGroupPage";
import React from "react";

export default function layout({ children }) {
	return <ViewGroupLayout>{children}</ViewGroupLayout>;
}
