/*
=================================================================
RULE: ALL FILE NAMES IN THE BUILDER-LAYER MUST START WITH "BUILD"
=================================================================
*/

export const buildNewFamilyMemberEntries = async (data) => {
	// console.log("Event: build entry function", data);

	const entries = [];

	entries.push(data);

	return entries;
};
