"use client";
import React from "react";

export default function GroupDiscoveryPage() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Group Discovery Page</div>;
}
