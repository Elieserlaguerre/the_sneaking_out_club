"use client";
import React from "react";

export default function ViewReportPage() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>View Report Page</div>;
}
