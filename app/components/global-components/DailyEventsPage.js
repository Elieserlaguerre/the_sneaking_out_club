"use client";
import React from "react";

export default function DailyEventsPage() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Daily Events Page</div>;
}
