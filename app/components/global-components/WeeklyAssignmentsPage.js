"use client";
import React from "react";

export default function WeeklyAssignmentsPage() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Weekly Assignments Page</div>;
}
