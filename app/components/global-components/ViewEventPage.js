"use client";
import React from "react";

export default function ViewEventPage() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>View Event Page</div>;
}
