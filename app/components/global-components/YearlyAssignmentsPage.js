"use client";
import React from "react";

export default function YearlyAssignmentsPage() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Yearly Assignments Page</div>;
}
