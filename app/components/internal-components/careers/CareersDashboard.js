"use client";
import React from "react";

export default function CareersDashboard() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Careers Dashboard</div>;
}
