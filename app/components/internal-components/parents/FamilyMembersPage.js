"use clients";
import React from "react";

export default function FamilyMembersPage() {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Family Members Page</div>;
}
