"use client";
import React from "react";

export default function MembersProfile({ userId }) {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Members Profile</div>;
}
