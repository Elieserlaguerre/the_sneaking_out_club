"use client";
import React from "react";

export default function ParentsProfile({ userId }) {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Parents Profile</div>;
}
