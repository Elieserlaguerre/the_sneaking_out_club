"use client";
import React from "react";

export default function CareersProfile({ userId }) {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>CareersProfile</div>;
}
