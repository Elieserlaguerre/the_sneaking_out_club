"use client";
import React from "react";

export default function TeachersProfile({ userId }) {
	function classNames(...classes) {
		return classes.filter(Boolean).join(" ");
	}

	return <div>Teachers Profile</div>;
}
